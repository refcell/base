# ADR NNNN: <short title>

- Status: proposed | accepted | superseded by ADR-XXXX
- Date: YYYY-MM-DD
- Deciders: <oracle model/id, reviewer model/id — must be a different model family than the oracle>

## Context

What forces (constraints, prior evidence, charter goal) make a decision necessary here?

## Decision

The single decision being made, stated as a concrete, checkable action.

## Alternatives considered

- Alternative A — why rejected.
- Alternative B — why rejected.

## Invariants affected

Which charter/RESUME invariants this touches (e.g., state ownership, one-writer-per-worktree,
signing) and how the decision preserves them.

## Validation evidence

Link to the `evidence/evidence.jsonl` row(s) (by `id`) that support this decision, or state
"none yet — validation pending in milestone <M#>" if the decision precedes implementation.

## Consequences

What becomes easier or harder afterward; what follow-up decisions this opens (add them to
`DECISIONS.md` if not already tracked).
