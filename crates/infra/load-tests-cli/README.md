# `base-load-tests-cli`

Reusable command-line interface for running Base load tests, draining test accounts, and rescuing stranded funds. It is shared by the unified `base load-test` command and the temporary `base-load-tester` migration wrapper.
