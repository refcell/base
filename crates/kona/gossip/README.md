# base-gossip

Gossipsub network driver for the OP Stack.

Ported from [`kona-gossip`](https://github.com/ethereum-optimism/kona).

## Features

- `metrics` - Prometheus metrics support

## Usage

```toml
[dependencies]
base-gossip = { workspace = true }
```
