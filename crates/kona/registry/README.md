# base-registry

A registry of superchain configurations.

Ported from [`kona-registry`](https://github.com/ethereum-optimism/kona).

## Features

- `std` - Standard library support
- `tabled` - Table display support

## Usage

```toml
[dependencies]
base-registry = { workspace = true }
```
