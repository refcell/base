# base-peers

Network peers library for the OP Stack.

Ported from [`kona-peers`](https://github.com/ethereum-optimism/kona).

## Features

- `arbitrary` - Arbitrary trait implementations for testing

## Usage

```toml
[dependencies]
base-peers = { workspace = true }
```
