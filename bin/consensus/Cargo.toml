[package]
name = "base-consensus"
description = "Base Consensus Binary"

version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
# Workspace
base-cli-utils.workspace = true
base-client-cli.workspace = true

# Base (ported from kona)
base-disc = { workspace = true, features = ["metrics"] }
base-derive = { workspace = true, features = ["metrics"] }
base-engine = { workspace = true, features = ["metrics"] }
base-genesis.workspace = true
base-gossip = { workspace = true, features = ["metrics"] }
base-node-service = { workspace = true, features = ["metrics"] }
base-providers-alloy = { workspace = true, features = ["metrics"] }
rollup-boost.workspace = true

# Alloy
alloy-rpc-types-engine.workspace = true

# Misc
url.workspace = true

# CLI
clap.workspace = true
eyre.workspace = true
strum.workspace = true

# Tracing
tracing.workspace = true

# Metrics
metrics.workspace = true

